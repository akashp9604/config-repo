server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/<your-username>/<your-private-repo>.git
          username: <your-github-username>
          password: <your-github-PAT>
          clone-on-start: true
